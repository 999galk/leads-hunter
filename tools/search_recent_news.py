# search_recent_news tool
# Searches for recent news about a given company (called twice by Copywriter:
# once for "DataStax IBM" and once for "ScyllaDB").
# Falls back to curated mock news if no API key is configured.
